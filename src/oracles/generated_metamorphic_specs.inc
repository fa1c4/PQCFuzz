// Generated by scripts/generate_oracle_specs.py. DO NOT EDIT.

static std::vector<MetamorphicSpec> GeneratedMetamorphicOracleSpecs() {
  std::vector<MetamorphicSpec> specs;
  {
    MetamorphicSpec spec;
    spec.oracle_id = "kem_decaps_c";
    spec.primitive_type = "kem";
    spec.field = "ciphertext";
    spec.expected_relation = "EXPECT_DIFFERENT";
    spec.finding_subclass = "ciphertext_malleability";
    spec.uses_rng = false;
    spec.metadata.claim = "A ciphertext mutation that changes the decapsulation input shall not be ignored by Decaps; the derived shared secret shall change.";
    spec.metadata.evidence_class = EvidenceClass::kNormative;
    spec.metadata.source_reference = "FIPS 203 Section 7.3 (ciphertext binding)";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"A single mutation may be a no-op at the algebraic level; the oracle records effectiveness", "Does not prove IND-CCA security"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the unmodified ciphertext decapsulates successfully";
    spec.metadata.negative_control = "the mutation record shows the ciphertext changed";
    specs.push_back(std::move(spec));
  }
  {
    MetamorphicSpec spec;
    spec.oracle_id = "kem_decaps_sk";
    spec.primitive_type = "kem";
    spec.field = "secret_key";
    spec.expected_relation = "EXPECT_DIFFERENT";
    spec.finding_subclass = "secret_key_malleability";
    spec.uses_rng = false;
    spec.metadata.claim = "A secret-key mutation that changes a used decapsulation input should change the derived shared secret.";
    spec.metadata.evidence_class = EvidenceClass::kEngineeringRecommendation;
    spec.metadata.source_reference = "DeepSeek design doc Section 38.4 (relational key mutations)";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"The generic oracle has a ~1/q per-position false-positive floor: when the ciphertext coefficient at the mutated secret position is 0 mod q, the shared secret provably cannot change", "kem_decaps_sk is not part of the security-tier oracle set"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the unmutated secret key decapsulates correctly";
    spec.metadata.negative_control = "the mutation record shows the secret key changed";
    specs.push_back(std::move(spec));
  }
  {
    MetamorphicSpec spec;
    spec.oracle_id = "kem_encaps_badrng";
    spec.primitive_type = "kem";
    spec.field = "rng";
    spec.expected_relation = "EXPECT_DIFFERENT";
    spec.finding_subclass = "encaps_rng_ignored";
    spec.uses_rng = true;
    spec.metadata.claim = "Encapsulation shall consume its random coins so that distinct randomness streams produce distinct ciphertexts or shared secrets.";
    spec.metadata.evidence_class = EvidenceClass::kReferenceDerived;
    spec.metadata.source_reference = "FIPS 203 Section 3.3 (encapsulation randomness)";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Tests distinct-stream consumption only, not reported RNG failure", "A deterministic PKE component is not the external ML-KEM contract"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the deterministic tape is observed to be consumed";
    spec.metadata.negative_control = "the baseline encapsulation succeeds";
    specs.push_back(std::move(spec));
  }
  {
    MetamorphicSpec spec;
    spec.oracle_id = "kem_encaps_pk_0";
    spec.primitive_type = "kem";
    spec.field = "public_key";
    spec.expected_relation = "EXPECT_DIFFERENT";
    spec.finding_subclass = "zero_public_key_equivalence";
    spec.uses_rng = false;
    spec.metadata.claim = "An encapsulation key with all-zero bytes should not produce the same encapsulation output as a valid key.";
    spec.metadata.evidence_class = EvidenceClass::kEngineeringRecommendation;
    spec.metadata.source_reference = "DeepSeek design doc Section 38.4 (key-mutation robustness)";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"The all-zero key is not a valid encapsulation key under FIPS 203; canonicality checks require the dedicated EK oracle", "Diagnostic robustness check only"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the untouched public key encapsulates";
    spec.metadata.negative_control = "the mutation record shows all key bytes zeroed";
    specs.push_back(std::move(spec));
  }
  {
    MetamorphicSpec spec;
    spec.oracle_id = "kem_encaps_pk";
    spec.primitive_type = "kem";
    spec.field = "public_key";
    spec.expected_relation = "EXPECT_DIFFERENT";
    spec.finding_subclass = "public_key_ignored_or_malleable";
    spec.uses_rng = false;
    spec.metadata.claim = "A mutated encapsulation key that changes the polynomial or seed fields should change the encapsulation output.";
    spec.metadata.evidence_class = EvidenceClass::kEngineeringRecommendation;
    spec.metadata.source_reference = "FIPS 203 Section 7.2 (encapsulation key input)";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Does not check canonical 12-bit encoding; that requires the dedicated EK canonicality oracle", "A mutation in an unused bit position may legitimately not change the output"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the untouched public key encapsulates";
    spec.metadata.negative_control = "the mutation record shows the public key changed";
    specs.push_back(std::move(spec));
  }
  {
    MetamorphicSpec spec;
    spec.oracle_id = "kem_keygen_badrng";
    spec.primitive_type = "kem";
    spec.field = "rng";
    spec.expected_relation = "EXPECT_DIFFERENT";
    spec.finding_subclass = "keygen_rng_ignored";
    spec.uses_rng = true;
    spec.metadata.claim = "Key generation shall consume its random seeds so that distinct randomness streams produce distinct keys.";
    spec.metadata.evidence_class = EvidenceClass::kReferenceDerived;
    spec.metadata.source_reference = "FIPS 203 Section 3.3 and Section 7.1 (key generation randomness)";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Tests distinct-stream consumption only, not reported RNG failure", "Low-entropy but successful RNG output is an RBG-health concern"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the deterministic tape is observed to be consumed";
    spec.metadata.negative_control = "the produced key pair is usable for a local round trip";
    specs.push_back(std::move(spec));
  }
  {
    MetamorphicSpec spec;
    spec.oracle_id = "sig_keygen_badrng";
    spec.primitive_type = "sig";
    spec.field = "rng";
    spec.expected_relation = "EXPECT_DIFFERENT";
    spec.finding_subclass = "keygen_rng_ignored";
    spec.uses_rng = true;
    spec.metadata.claim = "Signature key generation shall consume its random seeds so that distinct randomness streams produce distinct keys.";
    spec.metadata.evidence_class = EvidenceClass::kReferenceDerived;
    spec.metadata.source_reference = "FIPS 204 Section 3.6.1 (key generation randomness)";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Tests distinct-stream consumption only, not reported RNG failure", "Low-entropy but successful RNG output is an RBG-health concern"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the deterministic tape is observed to be consumed";
    spec.metadata.negative_control = "the produced key pair signs and verifies";
    specs.push_back(std::move(spec));
  }
  {
    MetamorphicSpec spec;
    spec.oracle_id = "sig_sign_badrng";
    spec.primitive_type = "sig";
    spec.field = "rng";
    spec.expected_relation = "EXPECT_DIFFERENT";
    spec.finding_subclass = "sign_rng_ignored";
    spec.uses_rng = true;
    spec.metadata.claim = "Hedged signing shall consume its random seed so that distinct randomness streams produce distinct signatures.";
    spec.metadata.evidence_class = EvidenceClass::kReferenceDerived;
    spec.metadata.source_reference = "FIPS 204 Section 3.4 (hedged signing)";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Deterministic profiles legitimately ignore ambient randomness", "Does not test reported RNG failure handling"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the deterministic tape is observed to be consumed";
    spec.metadata.negative_control = "the produced signature verifies";
    specs.push_back(std::move(spec));
  }
  {
    MetamorphicSpec spec;
    spec.oracle_id = "sig_sign_m";
    spec.primitive_type = "sig";
    spec.field = "message";
    spec.expected_relation = "EXPECT_DIFFERENT";
    spec.finding_subclass = "message_not_bound_in_signature_generation";
    spec.uses_rng = false;
    spec.metadata.claim = "A signing input mutation that changes the message should change the produced signature bytes.";
    spec.metadata.evidence_class = EvidenceClass::kEngineeringRecommendation;
    spec.metadata.source_reference = "FIPS 204 Section 5.2 (message binding)";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Hedged signing with fresh randomness also changes signatures, so the relation is only meaningful with controlled randomness", "Experimental oracle"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the baseline signature verifies";
    spec.metadata.negative_control = "the mutation record shows the message changed";
    specs.push_back(std::move(spec));
  }
  {
    MetamorphicSpec spec;
    spec.oracle_id = "sig_sign_sk";
    spec.primitive_type = "sig";
    spec.field = "secret_key";
    spec.expected_relation = "EXPECT_DIFFERENT";
    spec.finding_subclass = "secret_key_ignored_or_malleable";
    spec.uses_rng = false;
    spec.metadata.claim = "A signing secret-key mutation that changes the used key material should change the produced signature.";
    spec.metadata.evidence_class = EvidenceClass::kEngineeringRecommendation;
    spec.metadata.source_reference = "DeepSeek design doc Section 38.4 (relational key mutations)";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Mutations in unused secret-key fields legitimately may not change the output", "Experimental oracle"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the baseline signature verifies";
    spec.metadata.negative_control = "the mutation record shows the secret key changed";
    specs.push_back(std::move(spec));
  }
  {
    MetamorphicSpec spec;
    spec.oracle_id = "sig_verify_m";
    spec.primitive_type = "sig";
    spec.field = "message";
    spec.expected_relation = "EXPECT_DIFFERENT";
    spec.finding_subclass = "message_binding_failure";
    spec.uses_rng = false;
    spec.metadata.claim = "A signature that verifies for one message shall not verify for a distinct message.";
    spec.metadata.evidence_class = EvidenceClass::kNormative;
    spec.metadata.source_reference = "FIPS 204 Section 3.1 (EUF-CMA message binding)";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"A single held-out message mutation is not a forgery search", "Does not prove EUF-CMA"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the original message verifies";
    spec.metadata.negative_control = "the mutation record shows the message changed";
    specs.push_back(std::move(spec));
  }
  {
    MetamorphicSpec spec;
    spec.oracle_id = "sig_verify_sig";
    spec.primitive_type = "sig";
    spec.field = "signature";
    spec.expected_relation = "EXPECT_DIFFERENT";
    spec.finding_subclass = "signature_malleability";
    spec.uses_rng = false;
    spec.metadata.claim = "A mutated signature that verifies successfully is a critical signature-binding violation unless the mutation is a documented equivalent encoding.";
    spec.metadata.evidence_class = EvidenceClass::kNormative;
    spec.metadata.source_reference = "FIPS 204 Section 3.1 (sUF-CMA byte-string signature identity)";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Seed and mutation budgets bound the search; no forgery may be classified NO_COUNTEREXAMPLE, never a proof"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the unmodified signature verifies";
    spec.metadata.negative_control = "the mutation record shows the signature changed";
    specs.push_back(std::move(spec));
  }
  {
    MetamorphicSpec spec;
    spec.oracle_id = "sig_verify_pk";
    spec.primitive_type = "sig";
    spec.field = "public_key";
    spec.expected_relation = "EXPECT_DIFFERENT";
    spec.finding_subclass = "public_key_binding_failure";
    spec.uses_rng = false;
    spec.metadata.claim = "A signature shall not verify under a different public key.";
    spec.metadata.evidence_class = EvidenceClass::kNormative;
    spec.metadata.source_reference = "FIPS 204 Section 3.1 (public-key binding)";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Cross-key candidates require a valid alternate key; the oracle records the replay requirements"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the signature verifies under the original public key";
    spec.metadata.negative_control = "the alternate public key is a valid key from a second key pair";
    specs.push_back(std::move(spec));
  }
  return specs;
}
