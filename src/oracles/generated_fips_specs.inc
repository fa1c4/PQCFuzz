// Generated by scripts/generate_oracle_specs.py. DO NOT EDIT.

static std::vector<OracleSpec> GeneratedFipsOracleSpecs() {
  std::vector<OracleSpec> specs;
  {
    OracleSpec spec;
    spec.oracle_id = "mlkem_local_roundtrip";
    spec.algorithm_family = "ML-KEM";
    spec.primitive_type = "kem";
    spec.api_surface = {"keygen", "encaps", "decaps"};
    spec.mutation_type = "none";
    spec.mutation_targets = {};
    spec.expected_relation = ExpectedRelationFromName("SAME_SHARED_SECRET");
    spec.comparator = "bytes_equal";
    spec.triage_policy = "semantic_mismatch_requires_manual_review";
    spec.precondition_json = "{\"same_algorithm\":true,\"valid_lengths\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "For a valid ML-KEM key pair, Decaps(dk, Encaps(ek).ciphertext) shall equal Encaps(ek).shared_secret.";
    spec.metadata.evidence_class = EvidenceClass::kReferenceDerived;
    spec.metadata.source_reference = "FIPS 203 Sections 7.1-7.3 (external ML-KEM functions)";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Round-trip agreement is a functional relation, not an IND-CCA security result", "Finite testing cannot establish computational security"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "distinct randomness produces a distinct ciphertext";
    spec.metadata.negative_control = "a valid encapsulation always decapsulates on the same implementation";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "mlkem_cross_exchange_roundtrip";
    spec.algorithm_family = "ML-KEM";
    spec.primitive_type = "kem";
    spec.api_surface = {"keygen", "encaps", "decaps"};
    spec.mutation_type = "none";
    spec.mutation_targets = {};
    spec.expected_relation = ExpectedRelationFromName("SAME_SHARED_SECRET");
    spec.comparator = "bytes_equal";
    spec.triage_policy = "semantic_mismatch_requires_manual_review";
    spec.precondition_json = "{\"pair_alg_exchange_contract_required\":[\"public_key_exchange\",\"ciphertext_exchange\",\"secret_key_exchange\"]}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "Two conforming ML-KEM implementations exchanging encapsulation keys and ciphertexts shall derive the same shared secret.";
    spec.metadata.evidence_class = EvidenceClass::kReferenceDerived;
    spec.metadata.source_reference = "FIPS 203 Sections 7.2-7.3";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Requires the pair file to declare a compatible exchange contract", "A self-reference pair does not establish independent correctness"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "left-to-right and right-to-left exchange both reconstruct the encapsulated secret";
    spec.metadata.negative_control = "local round trips succeed on both implementations";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "mlkem_tampered_ciphertext_implicit_rejection";
    spec.algorithm_family = "ML-KEM";
    spec.primitive_type = "kem";
    spec.api_surface = {"keygen", "encaps", "decaps"};
    spec.mutation_type = "field_aware";
    spec.mutation_targets = {"ciphertext.u", "ciphertext.v"};
    spec.expected_relation = ExpectedRelationFromName("REJECT_OR_DIFFERENT_SHARED_SECRET");
    spec.comparator = "not_equal_to_original_shared_secret_or_reject";
    spec.triage_policy = "potential_crypto_vuln_if_original_secret_returned";
    spec.precondition_json = "{\"valid_ciphertext_before_mutation\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "A correctly sized ciphertext whose re-encryption does not match shall not yield the original encapsulated shared secret.";
    spec.metadata.evidence_class = EvidenceClass::kNormative;
    spec.metadata.source_reference = "FIPS 203 Sections 7.3 and 3.3 (implicit rejection)";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Does not prove IND-CCA security", "Fallback stability, z-separation, and no-reject-flag behavior require the dedicated implicit-rejection relations"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the unmodified ciphertext decapsulates to the encapsulated secret";
    spec.metadata.negative_control = "the mutation record shows the ciphertext bytes changed";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "mlkem_bad_randomness_sanity";
    spec.algorithm_family = "ML-KEM";
    spec.primitive_type = "kem";
    spec.api_surface = {"keygen", "encaps"};
    spec.mutation_type = "bad_randomness";
    spec.mutation_targets = {};
    spec.expected_relation = ExpectedRelationFromName("NO_CRASH");
    spec.comparator = "status_only";
    spec.triage_policy = "api_unsupported_is_not_failure";
    spec.precondition_json = "{\"rng_injection_available\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "ML-KEM key generation and encapsulation shall consume the supplied randomness so that distinct randomness streams produce distinct outputs.";
    spec.metadata.evidence_class = EvidenceClass::kReferenceDerived;
    spec.metadata.source_reference = "FIPS 203 Section 3.3 (randomness requirements)";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Does not test reported RNG failure handling (no output on failure)", "Does not measure RBG health or entropy quality"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "two distinct deterministic tapes are observed to be consumed";
    spec.metadata.negative_control = "the baseline encapsulation succeeds";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "mlkem_implicit_rejection_relations";
    spec.algorithm_family = "ML-KEM";
    spec.primitive_type = "kem";
    spec.api_surface = {"keygen", "encaps", "decaps"};
    spec.mutation_type = "field_aware";
    spec.mutation_targets = {"ciphertext.u", "ciphertext.v", "secret_key.z"};
    spec.expected_relation = ExpectedRelationFromName("REJECT_OR_DIFFERENT_SHARED_SECRET");
    spec.comparator = "relational_implicit_rejection";
    spec.triage_policy = "potential_crypto_vuln_if_fallback_not_bound_to_z";
    spec.precondition_json = "{\"rejection_secret_in_sk\":true,\"valid_ciphertext_before_mutation\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "For a correctly sized invalid ML-KEM ciphertext, decapsulation shall return a stable implicit-rejection secret that depends on the stored rejection secret z, while valid decapsulation shall not depend on z, and no public reject flag shall be exposed.";
    spec.metadata.evidence_class = EvidenceClass::kNormative;
    spec.metadata.source_reference = "FIPS 203 Sections 7.3 and 3.3; DeepSeek design doc Section 24 (implicit-rejection relations)";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Does not prove IND-CCA security", "A semantically neutral ciphertext mutation is recorded as no_effect rather than a finding"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the untouched valid ciphertext decapsulates to the encapsulated secret";
    spec.metadata.negative_control = "the mutation records show both the ciphertext and the z region changed";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "mlkem_raw_length_boundary";
    spec.algorithm_family = "ML-KEM";
    spec.primitive_type = "kem";
    spec.api_surface = {"keygen", "encaps", "decaps"};
    spec.mutation_type = "none";
    spec.mutation_targets = {};
    spec.expected_relation = ExpectedRelationFromName("NO_CRASH");
    spec.comparator = "not_applicable_at_raw_boundary";
    spec.triage_policy = "record_not_applicable_with_limitation";
    spec.precondition_json = "{\"valid_lengths\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "Exact ciphertext length enforcement for ML-KEM decapsulation is required by FIPS 203, but the raw fixed-pointer API used here has no ciphertext length parameter; enforcement must be provided by a length-aware wrapper.";
    spec.metadata.evidence_class = EvidenceClass::kNormative;
    spec.metadata.source_reference = "FIPS 203 Section 7.3; DeepSeek design doc Section 19 (length oracle boundary)";
    spec.metadata.scope_api_layer = "wrapper";
    spec.metadata.limitations = {"The fixed-pointer raw boundary cannot perform runtime length validation; this record is NOT_APPLICABLE, not a pass", "A wrapper-level exact-length oracle requires a length-aware adapter"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "a baseline encaps/decaps roundtrip succeeds";
    spec.metadata.negative_control = "the record is not persisted as a security finding";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "mlkem_ek_canonicality";
    spec.algorithm_family = "ML-KEM";
    spec.primitive_type = "kem";
    spec.api_surface = {"keygen", "encaps"};
    spec.mutation_type = "field_aware";
    spec.mutation_targets = {"public_key.t"};
    spec.expected_relation = ExpectedRelationFromName("REJECT_OR_DIFFERENT_SHARED_SECRET");
    spec.comparator = "encaps_rejects_noncanonical_12bit_coefficient";
    spec.triage_policy = "potential_crypto_vuln_if_noncanonical_public_key_accepted";
    spec.precondition_json = "{\"valid_public_key_before_mutation\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "ML-KEM encapsulation shall not proceed on an encapsulation key whose 12-bit polynomial coefficients are not canonical: q-1 is accepted, and q, q+1, and 4095 are rejected before encapsulation.";
    spec.metadata.evidence_class = EvidenceClass::kNormative;
    spec.metadata.source_reference = "FIPS 203 Sections 7.2 and 4.2 (encapsulation-key checks); DeepSeek design doc Sections 28.4 and 44";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Boundary positions sample the first, middle, and last coefficient of every polynomial plus a byte-straddling pair", "A wrapper that normalizes or reduces coefficients before encapsulation records accepted behavior as a finding", "Historical liboqs releases that ship pre-standardization Kyber (0.8.0 and 0.4.0) do not implement the FIPS 203 encapsulation-key checks; NONCONFORMANT verdicts on those targets record expected profile divergence, not a defect in a FIPS 203 implementation"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the unmodified encapsulation key encapsulates";
    spec.metadata.negative_control = "each mutation record shows the encoded coefficient changed to the boundary value";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "fips203_kat_keygen";
    spec.algorithm_family = "ML-KEM";
    spec.primitive_type = "kem";
    spec.api_surface = {"keygen"};
    spec.mutation_type = "none";
    spec.mutation_targets = {};
    spec.expected_relation = ExpectedRelationFromName("SAME_SHARED_SECRET");
    spec.comparator = "exact_bytes";
    spec.triage_policy = "reference_vector_mismatch_is_nonconformance";
    spec.precondition_json = "{\"kat_vectors_available\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "ML-KEM.KeyGen_internal(d, z) shall produce byte-identical ek and dk values for the NIST ACVP ML-KEM keyGen vectors.";
    spec.metadata.evidence_class = EvidenceClass::kReferenceDerived;
    spec.metadata.source_reference = "FIPS 203 Section 6.1 and Algorithm 16; NIST ACVP ML-KEM-keyGen-FIPS203 expectedResults";
    spec.metadata.scope_api_layer = "internal";
    spec.metadata.limitations = {"The checked-in table is a capped subset of the ACVP corpus", "Requires a pinned ACVP-Server commit and the reference adapter derand hook"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "a second run with the same d and z is byte-identical";
    spec.metadata.negative_control = "distinct d/z inputs produce distinct keys";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "fips203_kat_encaps";
    spec.algorithm_family = "ML-KEM";
    spec.primitive_type = "kem";
    spec.api_surface = {"encaps"};
    spec.mutation_type = "none";
    spec.mutation_targets = {};
    spec.expected_relation = ExpectedRelationFromName("SAME_SHARED_SECRET");
    spec.comparator = "exact_bytes";
    spec.triage_policy = "reference_vector_mismatch_is_nonconformance";
    spec.precondition_json = "{\"kat_vectors_available\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "ML-KEM.Encaps_internal(ek, m) shall produce the ACVP expected ciphertext c and shared secret k.";
    spec.metadata.evidence_class = EvidenceClass::kReferenceDerived;
    spec.metadata.source_reference = "FIPS 203 Section 6.2 and Algorithm 17; NIST ACVP ML-KEM-encapDecap-FIPS203 expectedResults";
    spec.metadata.scope_api_layer = "internal";
    spec.metadata.limitations = {"Capped ACVP subset; requires the reference adapter encaps_derand hook"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the same deterministic coins reproduce the same ciphertext";
    spec.metadata.negative_control = "the baseline public key is a fixed ACVP vector";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "fips203_kat_decaps";
    spec.algorithm_family = "ML-KEM";
    spec.primitive_type = "kem";
    spec.api_surface = {"decaps"};
    spec.mutation_type = "none";
    spec.mutation_targets = {};
    spec.expected_relation = ExpectedRelationFromName("SAME_SHARED_SECRET");
    spec.comparator = "exact_bytes";
    spec.triage_policy = "reference_vector_mismatch_is_nonconformance";
    spec.precondition_json = "{\"kat_vectors_available\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "ML-KEM.Decaps(dk, c) shall produce the ACVP expected shared secret k.";
    spec.metadata.evidence_class = EvidenceClass::kReferenceDerived;
    spec.metadata.source_reference = "FIPS 203 Section 6.3 and Algorithm 18; NIST ACVP ML-KEM-encapDecap-FIPS203 expectedResults";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Capped ACVP subset"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the expected shared secret equals the encaps vector k";
    spec.metadata.negative_control = "a different ciphertext changes the derived secret";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "mlkem_rng_failure";
    spec.algorithm_family = "ML-KEM";
    spec.primitive_type = "kem";
    spec.api_surface = {"keygen", "encaps"};
    spec.mutation_type = "bad_randomness";
    spec.mutation_targets = {};
    spec.expected_relation = ExpectedRelationFromName("NO_CRASH");
    spec.comparator = "no_output_on_reported_rng_failure";
    spec.triage_policy = "hardening_gap_when_void_rng_cannot_report";
    spec.precondition_json = "{\"rng_failure_injection_available\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "A reported RNG failure during ML-KEM key generation or encapsulation should produce a failure indication and no cryptographic output rather than proceeding with zeroed randomness.";
    spec.metadata.evidence_class = EvidenceClass::kEngineeringRecommendation;
    spec.metadata.source_reference = "FIPS 203 Section 3.3 and DeepSeek design doc Sections 22 and 28.9";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Void randombytes APIs cannot report failure; continuing with zeroed coins is a hardening gap, not a normative nonconformance", "The fault injector signals failure through the PQCFuzz RNG layer, not through the operating-system RNG"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the same call succeeds without the injected failure";
    spec.metadata.negative_control = "the failure tape is observed to be consumed";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "alloc_failure_contract";
    spec.algorithm_family = "ML-KEM";
    spec.primitive_type = "kem";
    spec.api_surface = {"keygen"};
    spec.mutation_type = "allocator_failure";
    spec.mutation_targets = {};
    spec.expected_relation = ExpectedRelationFromName("NO_OUTPUT_ON_ALLOCATION_FAILURE");
    spec.comparator = "no_output_or_partial_output_check";
    spec.triage_policy = "hardening_gap_for_undocumented_allocation_failure_contract";
    spec.precondition_json = "{\"allocator_fault_injection_built\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "When an allocation fails during ML-KEM key generation, the call should report a failure and leave caller output buffers untouched, with no partial key material exposed.";
    spec.metadata.evidence_class = EvidenceClass::kEngineeringRecommendation;
    spec.metadata.source_reference = "DeepSeek design doc Sections 25 and 36.7 (allocator fault injector and failure-state contract)";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Requires a binary built with -Wl,--wrap=malloc/calloc/realloc", "Libraries that do not document allocation-failure behavior are reported as HARDENING_GAP, not as normative nonconformance"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "key generation succeeds without injected allocation failure";
    spec.metadata.negative_control = "each subtest records the number of triggered allocation failures";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "mldsa_local_sign_verify";
    spec.algorithm_family = "ML-DSA";
    spec.primitive_type = "sig";
    spec.api_surface = {"keygen", "sign", "verify"};
    spec.mutation_type = "none";
    spec.mutation_targets = {};
    spec.expected_relation = ExpectedRelationFromName("VERIFY_TRUE");
    spec.comparator = "verify_accepts";
    spec.triage_policy = "semantic_mismatch_requires_manual_review";
    spec.precondition_json = "{\"ctx_len_lte_255\":true,\"valid_lengths\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "A signature produced by ML-DSA Sign with a valid key and context shall verify under the matching public key.";
    spec.metadata.evidence_class = EvidenceClass::kReferenceDerived;
    spec.metadata.source_reference = "FIPS 204 Sections 5.2-5.3 (external ML-DSA functions)";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Verification success is a functional relation, not a EUF-CMA security result"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the signature is produced from a freshly generated key pair";
    spec.metadata.negative_control = "a one-byte signature mutation is rejected by the negative oracle";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "mldsa_cross_verify";
    spec.algorithm_family = "ML-DSA";
    spec.primitive_type = "sig";
    spec.api_surface = {"keygen", "sign", "verify"};
    spec.mutation_type = "none";
    spec.mutation_targets = {};
    spec.expected_relation = ExpectedRelationFromName("VERIFY_TRUE");
    spec.comparator = "verify_accepts";
    spec.triage_policy = "semantic_mismatch_requires_manual_review";
    spec.precondition_json = "{\"ctx_len_lte_255\":true,\"pair_alg_exchange_contract_required\":[\"public_key_exchange\",\"signature_exchange\"]}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "Two conforming ML-DSA implementations exchanging public keys and signatures shall cross-verify for the same profile and context.";
    spec.metadata.evidence_class = EvidenceClass::kReferenceDerived;
    spec.metadata.source_reference = "FIPS 204 Sections 5.2-5.3";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Cross-verification must use an identical pure/prehash and context profile", "A self-reference pair does not establish independent correctness"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "both local sign/verify paths succeed";
    spec.metadata.negative_control = "a signature from a different key does not verify";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "mldsa_mutated_signature_negative";
    spec.algorithm_family = "ML-DSA";
    spec.primitive_type = "sig";
    spec.api_surface = {"keygen", "sign", "verify"};
    spec.mutation_type = "field_aware";
    spec.mutation_targets = {"signature.c", "signature.z", "signature.h"};
    spec.expected_relation = ExpectedRelationFromName("VERIFY_FALSE_OR_DECODE_REJECT_OR_API_INVALID_INPUT");
    spec.comparator = "verify_rejects_or_decode_rejects";
    spec.triage_policy = "potential_crypto_vuln_if_mutated_signature_verifies";
    spec.precondition_json = "{\"valid_signature_before_mutation\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "A mutated ML-DSA signature component shall not verify under the signing public key.";
    spec.metadata.evidence_class = EvidenceClass::kNormative;
    spec.metadata.source_reference = "FIPS 204 Sections 3.6.2, 5.3, and Algorithm 27 sigDecode";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Exact-length, hint canonicality, and norm boundaries require their dedicated oracles", "This oracle does not prove sUF-CMA"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the unmodified signature verifies";
    spec.metadata.negative_control = "the mutation record shows the signature bytes changed";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "mldsa_mutated_message_negative";
    spec.algorithm_family = "ML-DSA";
    spec.primitive_type = "sig";
    spec.api_surface = {"keygen", "sign", "verify"};
    spec.mutation_type = "message_mutation";
    spec.mutation_targets = {};
    spec.expected_relation = ExpectedRelationFromName("VERIFY_FALSE");
    spec.comparator = "verify_rejects";
    spec.triage_policy = "potential_crypto_vuln_if_mutated_message_verifies";
    spec.precondition_json = "{\"valid_signature_before_mutation\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "A signature shall not verify for a message that differs from the signed message.";
    spec.metadata.evidence_class = EvidenceClass::kNormative;
    spec.metadata.source_reference = "FIPS 204 Section 3.1 (message binding)";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Message equality in the verifier domain is not tested across all message encodings"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the original message verifies";
    spec.metadata.negative_control = "the mutation record shows the message bytes changed";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "mldsa_mutated_context_negative";
    spec.algorithm_family = "ML-DSA";
    spec.primitive_type = "sig";
    spec.api_surface = {"keygen", "sign", "verify"};
    spec.mutation_type = "context_mutation";
    spec.mutation_targets = {"ctx"};
    spec.expected_relation = ExpectedRelationFromName("VERIFY_FALSE_OR_API_UNSUPPORTED");
    spec.comparator = "verify_rejects_or_api_unsupported";
    spec.triage_policy = "potential_crypto_vuln_if_mutated_context_verifies";
    spec.precondition_json = "{\"ctx_len_lte_255\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "A signature shall not verify when the verification context differs from the signing context.";
    spec.metadata.evidence_class = EvidenceClass::kNormative;
    spec.metadata.source_reference = "FIPS 204 Section 5.2 (context string domain separation)";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Adapters without context support return API_UNSUPPORTED and are not failures", "Context boundary lengths 0/1/254/255/256 are not covered by this oracle"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the original context verifies";
    spec.metadata.negative_control = "the mutation record shows the context bytes changed";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "mldsa_oid_field_mutation_sanity";
    spec.algorithm_family = "ML-DSA";
    spec.primitive_type = "sig";
    spec.api_surface = {"sign", "verify"};
    spec.mutation_type = "field_aware";
    spec.mutation_targets = {"oid"};
    spec.expected_relation = ExpectedRelationFromName("VERIFY_FALSE_OR_API_UNSUPPORTED");
    spec.comparator = "verify_rejects_or_api_unsupported";
    spec.triage_policy = "api_unsupported_is_not_failure";
    spec.precondition_json = "{\"oid_field_present_in_input_model\":true}";
    spec.enabled_by_default = false;
    spec.requires_api = "HashML-DSA";
    spec.metadata.claim = "HashML-DSA verification shall reject a prehash object whose OID differs from the signing OID.";
    spec.metadata.evidence_class = EvidenceClass::kNormative;
    spec.metadata.source_reference = "FIPS 204 Section 5.4 (prehash OID binding)";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Disabled until a HashML-DSA prehash interface is available", "OID substitution must not be simulated by appending bytes to the context"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the matching OID verifies";
    spec.metadata.negative_control = "the mutation record shows the OID bytes changed";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "mldsa_bad_randomness_sanity";
    spec.algorithm_family = "ML-DSA";
    spec.primitive_type = "sig";
    spec.api_surface = {"sign"};
    spec.mutation_type = "bad_randomness";
    spec.mutation_targets = {};
    spec.expected_relation = ExpectedRelationFromName("NO_CRASH");
    spec.comparator = "status_only";
    spec.triage_policy = "api_unsupported_is_not_failure";
    spec.precondition_json = "{\"seeded_sign_available\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "Hedged ML-DSA signing shall consume the supplied randomness so that distinct randomness streams produce distinct signatures.";
    spec.metadata.evidence_class = EvidenceClass::kReferenceDerived;
    spec.metadata.source_reference = "FIPS 204 Section 3.4 (hedged signing randomness)";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Deterministic profiles legitimately ignore ambient randomness", "Reported RNG failure handling is a separate oracle"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "distinct deterministic tapes are observed to be consumed";
    spec.metadata.negative_control = "the baseline signature verifies";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "mldsa_verify_exact_lengths";
    spec.algorithm_family = "ML-DSA";
    spec.primitive_type = "sig";
    spec.api_surface = {"keygen", "sign", "verify"};
    spec.mutation_type = "field_aware";
    spec.mutation_targets = {"signature"};
    spec.expected_relation = ExpectedRelationFromName("VERIFY_FALSE_OR_DECODE_REJECT_OR_API_INVALID_INPUT");
    spec.comparator = "verify_rejects_non_exact_length";
    spec.triage_policy = "potential_crypto_vuln_if_non_exact_length_accepted";
    spec.precondition_json = "{\"valid_signature_before_mutation\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "ML-DSA verification shall return false for every signature length that differs from the profile signature length, including empty, L-1, L+1 with 0x00, L+1 with 0xA5, and oversized signatures.";
    spec.metadata.evidence_class = EvidenceClass::kNormative;
    spec.metadata.source_reference = "FIPS 204 Sections 3.6.2 and 5.3; DeepSeek design doc Section 29.3";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Public-key exact length is enforced by the wrapper when the raw API has no pk length parameter", "Truncations at every component boundary are sampled, not exhaustive"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the exact-length signature verifies";
    spec.metadata.negative_control = "each boundary mutation record shows a changed signature length";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "mldsa_ctx_boundaries";
    spec.algorithm_family = "ML-DSA";
    spec.primitive_type = "sig";
    spec.api_surface = {"keygen", "sign", "verify"};
    spec.mutation_type = "context_mutation";
    spec.mutation_targets = {"ctx"};
    spec.expected_relation = ExpectedRelationFromName("VERIFY_FALSE_OR_API_UNSUPPORTED");
    spec.comparator = "ctx_boundary_contract";
    spec.triage_policy = "potential_crypto_vuln_if_context_limit_not_enforced";
    spec.precondition_json = "{\"context_api_available\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "ML-DSA context lengths 0, 1, 254, and 255 shall sign and verify, context length 256 shall be rejected at the signing boundary, and a different verification context shall not verify.";
    spec.metadata.evidence_class = EvidenceClass::kNormative;
    spec.metadata.source_reference = "FIPS 204 Section 5.2 (context string) and DeepSeek design doc Section 29.8";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Adapters without an extended-context boundary record NOT_APPLICABLE", "Applications that never expose context are out of scope for the boundary subtest"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the empty-context signature verifies";
    spec.metadata.negative_control = "a mismatched context does not verify";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "mldsa_hint_canonicality";
    spec.algorithm_family = "ML-DSA";
    spec.primitive_type = "sig";
    spec.api_surface = {"keygen", "sign", "verify"};
    spec.mutation_type = "field_aware";
    spec.mutation_targets = {"signature.h"};
    spec.expected_relation = ExpectedRelationFromName("VERIFY_FALSE_OR_DECODE_REJECT_OR_API_INVALID_INPUT");
    spec.comparator = "verify_rejects_noncanonical_hint";
    spec.triage_policy = "potential_crypto_vuln_if_noncanonical_hint_verifies";
    spec.precondition_json = "{\"valid_signature_before_mutation\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "ML-DSA verification shall reject non-canonical hint encodings: cumulative counts that decrease or exceed omega, non-increasing positions within a polynomial, and non-zero unused position bytes.";
    spec.metadata.evidence_class = EvidenceClass::kNormative;
    spec.metadata.source_reference = "FIPS 204 Section 7.2 Algorithm 21 HintBitUnpack; DeepSeek design doc Section 29.4";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Each mutation class is applied once to a valid signature; a survivor is a test-gap signal, not a forgery", "All three parameter sets share the same mutation classes"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the unmodified signature verifies";
    spec.metadata.negative_control = "each mutation record shows the hint bytes changed";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "mldsa_z_norm_boundary";
    spec.algorithm_family = "ML-DSA";
    spec.primitive_type = "sig";
    spec.api_surface = {"keygen", "sign", "verify"};
    spec.mutation_type = "field_aware";
    spec.mutation_targets = {"signature.z"};
    spec.expected_relation = ExpectedRelationFromName("VERIFY_FALSE_OR_DECODE_REJECT_OR_API_INVALID_INPUT");
    spec.comparator = "verify_rejects_out_of_norm_z";
    spec.triage_policy = "potential_crypto_vuln_if_out_of_norm_z_verifies";
    spec.precondition_json = "{\"valid_signature_before_mutation\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "ML-DSA verification shall reject signatures whose response coefficients reach or exceed the norm bound gamma1 - beta, including the negative boundary.";
    spec.metadata.evidence_class = EvidenceClass::kNormative;
    spec.metadata.source_reference = "FIPS 204 Section 5.3 (norm check) and DeepSeek design doc Section 29.6";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"The valid-boundary mutation (gamma1 - beta - 1) breaks the challenge relation, so a rejection there is expected for an unrelated reason; that subtest is a structural observation", "Only the first packed response coefficient is rewritten"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the unmodified signature verifies";
    spec.metadata.negative_control = "each mutation record shows the packed coefficient changed";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "mldsa_rnd_determinism";
    spec.algorithm_family = "ML-DSA";
    spec.primitive_type = "sig";
    spec.api_surface = {"keygen", "sign"};
    spec.mutation_type = "none";
    spec.mutation_targets = {};
    spec.expected_relation = ExpectedRelationFromName("VERIFY_TRUE");
    spec.comparator = "relational_randomness";
    spec.triage_policy = "potential_crypto_vuln_if_randomness_ignored";
    spec.precondition_json = "{\"rng_injection_available\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "Hedged ML-DSA signing shall reproduce byte-identical signatures under a fixed zero randomness tape, and distinct randomness tapes shall produce distinct signatures that every verify.";
    spec.metadata.evidence_class = EvidenceClass::kReferenceDerived;
    spec.metadata.source_reference = "FIPS 204 Section 3.4 (hedged and deterministic signing) and DeepSeek design doc Section 29.7";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"The fixed zero tape is an external approximation of the internal fixed-rnd validation interface", "Deterministic profiles legitimately skip the fresh-randomness subtest"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the fixed-rnd signature verifies";
    spec.metadata.negative_control = "distinct tapes are observed to be consumed";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "mldsa_pure_prehash_separation";
    spec.algorithm_family = "ML-DSA";
    spec.primitive_type = "sig";
    spec.api_surface = {"sign", "verify"};
    spec.mutation_type = "none";
    spec.mutation_targets = {};
    spec.expected_relation = ExpectedRelationFromName("VERIFY_FALSE_OR_API_UNSUPPORTED");
    spec.comparator = "not_applicable_with_limitation";
    spec.triage_policy = "record_not_applicable";
    spec.precondition_json = "{\"prehash_api_available\":false}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "A pure ML-DSA signature and a prehash ML-DSA signature for the same message shall not verify in each other's domain.";
    spec.metadata.evidence_class = EvidenceClass::kNormative;
    spec.metadata.source_reference = "FIPS 204 Section 5.4 and DeepSeek design doc Section 29.8";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"No prehash interface is exposed by the selected adapters; recorded NOT_APPLICABLE rather than simulated"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "a pure signature verifies in the pure domain";
    spec.metadata.negative_control = "the record is not persisted as a finding";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "mldsa_ph_oid_separation";
    spec.algorithm_family = "ML-DSA";
    spec.primitive_type = "sig";
    spec.api_surface = {"sign", "verify"};
    spec.mutation_type = "none";
    spec.mutation_targets = {};
    spec.expected_relation = ExpectedRelationFromName("VERIFY_FALSE_OR_API_UNSUPPORTED");
    spec.comparator = "not_applicable_with_limitation";
    spec.triage_policy = "record_not_applicable";
    spec.precondition_json = "{\"prehash_api_available\":false}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "Prehash verification shall reject a digest whose OID differs from the signing OID.";
    spec.metadata.evidence_class = EvidenceClass::kNormative;
    spec.metadata.source_reference = "FIPS 204 Section 5.4 and DeepSeek design doc Section 29.8";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"No prehash/OID interface is exposed; recorded NOT_APPLICABLE"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "a matching prehash OID verifies when a prehash API exists";
    spec.metadata.negative_control = "the record is not persisted as a finding";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "mldsa_rng_failure";
    spec.algorithm_family = "ML-DSA";
    spec.primitive_type = "sig";
    spec.api_surface = {"keygen", "sign"};
    spec.mutation_type = "bad_randomness";
    spec.mutation_targets = {};
    spec.expected_relation = ExpectedRelationFromName("NO_CRASH");
    spec.comparator = "no_output_on_reported_rng_failure";
    spec.triage_policy = "hardening_gap_when_void_rng_cannot_report";
    spec.precondition_json = "{\"rng_failure_injection_available\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "A reported RNG failure during ML-DSA key generation or signing should produce a failure indication and no signature output rather than proceeding with zeroed randomness.";
    spec.metadata.evidence_class = EvidenceClass::kEngineeringRecommendation;
    spec.metadata.source_reference = "FIPS 204 Sections 3.4 and 3.6 and DeepSeek design doc Sections 22 and 28.9";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Void RNG APIs cannot report failure; continuing is a hardening gap"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the same call succeeds without the injected failure";
    spec.metadata.negative_control = "the failure tape is observed to be consumed";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "slhdsa_local_sign_verify";
    spec.algorithm_family = "SLH-DSA";
    spec.primitive_type = "sig";
    spec.api_surface = {"keygen", "sign", "verify"};
    spec.mutation_type = "none";
    spec.mutation_targets = {};
    spec.expected_relation = ExpectedRelationFromName("VERIFY_TRUE");
    spec.comparator = "verify_accepts";
    spec.triage_policy = "semantic_mismatch_requires_manual_review";
    spec.precondition_json = "{\"ctx_len_lte_255\":true,\"valid_lengths\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "A signature produced by SLH-DSA Sign with a valid key and context shall verify under the matching public key.";
    spec.metadata.evidence_class = EvidenceClass::kReferenceDerived;
    spec.metadata.source_reference = "FIPS 205 Sections 10-11 (external SLH-DSA functions)";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Verification success is a functional relation, not a EUF-CMA security result"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the signature is produced from a freshly generated key pair";
    spec.metadata.negative_control = "a one-byte signature mutation is rejected by the negative oracle";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "slhdsa_cross_verify";
    spec.algorithm_family = "SLH-DSA";
    spec.primitive_type = "sig";
    spec.api_surface = {"keygen", "sign", "verify"};
    spec.mutation_type = "none";
    spec.mutation_targets = {};
    spec.expected_relation = ExpectedRelationFromName("VERIFY_TRUE");
    spec.comparator = "verify_accepts";
    spec.triage_policy = "semantic_mismatch_requires_manual_review";
    spec.precondition_json = "{\"ctx_len_lte_255\":true,\"pair_alg_exchange_contract_required\":[\"public_key_exchange\",\"signature_exchange\"]}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "Two conforming SLH-DSA implementations exchanging public keys and signatures shall cross-verify for the same parameter set and context.";
    spec.metadata.evidence_class = EvidenceClass::kReferenceDerived;
    spec.metadata.source_reference = "FIPS 205 Sections 10-11";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Cross-verification requires an identical hash family, pure/prehash mode, and context", "A self-reference pair does not establish independent correctness"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "both local sign/verify paths succeed";
    spec.metadata.negative_control = "a signature from a different key does not verify";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "slhdsa_mutated_signature_negative";
    spec.algorithm_family = "SLH-DSA";
    spec.primitive_type = "sig";
    spec.api_surface = {"keygen", "sign", "verify"};
    spec.mutation_type = "field_aware";
    spec.mutation_targets = {"signature.R", "signature.SIGFORS", "signature.SIGHT", "signature.WOTS", "signature.XMSS_AUTH_PATH"};
    spec.expected_relation = ExpectedRelationFromName("VERIFY_FALSE_OR_DECODE_REJECT_OR_API_INVALID_INPUT");
    spec.comparator = "verify_rejects_or_decode_rejects";
    spec.triage_policy = "potential_crypto_vuln_if_mutated_signature_verifies";
    spec.precondition_json = "{\"valid_signature_before_mutation\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "A mutated SLH-DSA signature component (R, FORS, hypertree root, WOTS, or authentication path) shall not verify.";
    spec.metadata.evidence_class = EvidenceClass::kNormative;
    spec.metadata.source_reference = "FIPS 205 Sections 10.2 and 30.9-style component mutation requirements";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Component-internal address-format and root-regeneration checks require component oracles", "This oracle does not prove EUF-CMA"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the unmodified signature verifies";
    spec.metadata.negative_control = "the mutation record shows the signature bytes changed";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "slhdsa_mutated_message_negative";
    spec.algorithm_family = "SLH-DSA";
    spec.primitive_type = "sig";
    spec.api_surface = {"keygen", "sign", "verify"};
    spec.mutation_type = "message_mutation";
    spec.mutation_targets = {};
    spec.expected_relation = ExpectedRelationFromName("VERIFY_FALSE");
    spec.comparator = "verify_rejects";
    spec.triage_policy = "potential_crypto_vuln_if_mutated_message_verifies";
    spec.precondition_json = "{\"valid_signature_before_mutation\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "A signature shall not verify for a message that differs from the signed message.";
    spec.metadata.evidence_class = EvidenceClass::kNormative;
    spec.metadata.source_reference = "FIPS 205 Section 10.2 (message binding)";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Message equality in the verifier domain is not tested across all message encodings"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the original message verifies";
    spec.metadata.negative_control = "the mutation record shows the message bytes changed";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "slhdsa_mutated_context_negative";
    spec.algorithm_family = "SLH-DSA";
    spec.primitive_type = "sig";
    spec.api_surface = {"keygen", "sign", "verify"};
    spec.mutation_type = "context_mutation";
    spec.mutation_targets = {"ctx"};
    spec.expected_relation = ExpectedRelationFromName("VERIFY_FALSE_OR_API_UNSUPPORTED");
    spec.comparator = "verify_rejects_or_api_unsupported";
    spec.triage_policy = "potential_crypto_vuln_if_mutated_context_verifies";
    spec.precondition_json = "{\"ctx_len_lte_255\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "A signature shall not verify when the verification context differs from the signing context.";
    spec.metadata.evidence_class = EvidenceClass::kNormative;
    spec.metadata.source_reference = "FIPS 205 Sections 10.2 and 11 (context domain separation)";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Adapters without context support return API_UNSUPPORTED and are not failures", "Context boundary lengths 0/1/254/255/256 are not covered by this oracle"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the original context verifies";
    spec.metadata.negative_control = "the mutation record shows the context bytes changed";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "slhdsa_bad_randomness_sanity";
    spec.algorithm_family = "SLH-DSA";
    spec.primitive_type = "sig";
    spec.api_surface = {"keygen", "sign"};
    spec.mutation_type = "bad_randomness";
    spec.mutation_targets = {};
    spec.expected_relation = ExpectedRelationFromName("NO_CRASH");
    spec.comparator = "status_only";
    spec.triage_policy = "api_unsupported_is_not_failure";
    spec.precondition_json = "{\"rng_injection_available\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "Hedged SLH-DSA signing shall consume the supplied opt_rand so that distinct randomness streams produce distinct signatures.";
    spec.metadata.evidence_class = EvidenceClass::kReferenceDerived;
    spec.metadata.source_reference = "FIPS 205 Sections 9.2 and 11 (hedged signing)";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Deterministic profiles legitimately ignore ambient randomness", "Reported RNG failure handling is a separate oracle"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "distinct deterministic tapes are observed to be consumed";
    spec.metadata.negative_control = "the baseline signature verifies";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "slhdsa_verify_exact_lengths";
    spec.algorithm_family = "SLH-DSA";
    spec.primitive_type = "sig";
    spec.api_surface = {"keygen", "sign", "verify"};
    spec.mutation_type = "field_aware";
    spec.mutation_targets = {"signature"};
    spec.expected_relation = ExpectedRelationFromName("VERIFY_FALSE_OR_DECODE_REJECT_OR_API_INVALID_INPUT");
    spec.comparator = "verify_rejects_non_exact_length";
    spec.triage_policy = "potential_crypto_vuln_if_non_exact_length_accepted";
    spec.precondition_json = "{\"valid_signature_before_mutation\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "SLH-DSA verification shall return false for every signature length that differs from the parameter-set signature length.";
    spec.metadata.evidence_class = EvidenceClass::kNormative;
    spec.metadata.source_reference = "FIPS 205 Section 10.2; DeepSeek design doc Section 30.3";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Truncations at R, FORS, and hypertree boundaries are sampled by separate component oracles", "A wrapper that clamps the length records NOT_APPLICABLE"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the exact-length signature verifies";
    spec.metadata.negative_control = "each boundary mutation record shows a changed signature length";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "slhdsa_ctx_boundaries";
    spec.algorithm_family = "SLH-DSA";
    spec.primitive_type = "sig";
    spec.api_surface = {"keygen", "sign", "verify"};
    spec.mutation_type = "context_mutation";
    spec.mutation_targets = {"ctx"};
    spec.expected_relation = ExpectedRelationFromName("VERIFY_FALSE_OR_API_UNSUPPORTED");
    spec.comparator = "ctx_boundary_contract";
    spec.triage_policy = "potential_crypto_vuln_if_context_limit_not_enforced";
    spec.precondition_json = "{\"context_api_available\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "SLH-DSA context lengths 0, 1, 254, and 255 shall sign and verify, context length 256 shall be rejected, and a different verification context shall not verify.";
    spec.metadata.evidence_class = EvidenceClass::kNormative;
    spec.metadata.source_reference = "FIPS 205 Sections 10.2 and 11; DeepSeek design doc Section 30.7";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Adapters without an extended-context boundary record NOT_APPLICABLE"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the empty-context signature verifies";
    spec.metadata.negative_control = "a mismatched context does not verify";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "fips205_kat_keygen";
    spec.algorithm_family = "SLH-DSA";
    spec.primitive_type = "sig";
    spec.api_surface = {"keygen"};
    spec.mutation_type = "none";
    spec.mutation_targets = {};
    spec.expected_relation = ExpectedRelationFromName("VERIFY_TRUE");
    spec.comparator = "exact_bytes";
    spec.triage_policy = "reference_vector_mismatch_is_nonconformance";
    spec.precondition_json = "{\"kat_vectors_available\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "SLH-DSA seed-based key generation shall produce the ACVP expected public and private keys from the declared seed components.";
    spec.metadata.evidence_class = EvidenceClass::kReferenceDerived;
    spec.metadata.source_reference = "FIPS 205 Sections 9.1 and 11; NIST ACVP SLH-DSA-keyGen-FIPS205 expectedResults";
    spec.metadata.scope_api_layer = "internal";
    spec.metadata.limitations = {"Requires a seed-keypair hook on the reference adapter", "Capped ACVP subset"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the same seed reproduces the same key pair";
    spec.metadata.negative_control = "a different seed changes the key pair";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "slhdsa_pure_prehash_separation";
    spec.algorithm_family = "SLH-DSA";
    spec.primitive_type = "sig";
    spec.api_surface = {"sign", "verify"};
    spec.mutation_type = "none";
    spec.mutation_targets = {};
    spec.expected_relation = ExpectedRelationFromName("VERIFY_FALSE_OR_API_UNSUPPORTED");
    spec.comparator = "not_applicable_with_limitation";
    spec.triage_policy = "record_not_applicable";
    spec.precondition_json = "{\"prehash_api_available\":false}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "A pure SLH-DSA signature and a prehash SLH-DSA signature for the same message shall not verify in each other's domain.";
    spec.metadata.evidence_class = EvidenceClass::kNormative;
    spec.metadata.source_reference = "FIPS 205 Sections 10.2 and 11 and DeepSeek design doc Section 30.7";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"No prehash interface is exposed by the selected adapters; recorded NOT_APPLICABLE"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "a pure signature verifies in the pure domain";
    spec.metadata.negative_control = "the record is not persisted as a finding";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "slhdsa_ph_oid_separation";
    spec.algorithm_family = "SLH-DSA";
    spec.primitive_type = "sig";
    spec.api_surface = {"sign", "verify"};
    spec.mutation_type = "none";
    spec.mutation_targets = {};
    spec.expected_relation = ExpectedRelationFromName("VERIFY_FALSE_OR_API_UNSUPPORTED");
    spec.comparator = "not_applicable_with_limitation";
    spec.triage_policy = "record_not_applicable";
    spec.precondition_json = "{\"prehash_api_available\":false}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "Prehash SLH-DSA verification shall reject a digest whose OID or XOF output length differs from the signing parameters.";
    spec.metadata.evidence_class = EvidenceClass::kNormative;
    spec.metadata.source_reference = "FIPS 205 Sections 10.2 and 11 and DeepSeek design doc Section 30.7";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"No prehash/OID interface is exposed; recorded NOT_APPLICABLE"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "a matching prehash OID verifies when a prehash API exists";
    spec.metadata.negative_control = "the record is not persisted as a finding";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "slhdsa_rng_failure";
    spec.algorithm_family = "SLH-DSA";
    spec.primitive_type = "sig";
    spec.api_surface = {"keygen", "sign"};
    spec.mutation_type = "bad_randomness";
    spec.mutation_targets = {};
    spec.expected_relation = ExpectedRelationFromName("NO_CRASH");
    spec.comparator = "no_output_on_reported_rng_failure";
    spec.triage_policy = "hardening_gap_when_void_rng_cannot_report";
    spec.precondition_json = "{\"rng_failure_injection_available\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "A reported RNG failure during SLH-DSA key generation or signing should produce a failure indication and no signature output rather than proceeding with zeroed randomness.";
    spec.metadata.evidence_class = EvidenceClass::kEngineeringRecommendation;
    spec.metadata.source_reference = "FIPS 205 Section 3.1 and DeepSeek design doc Sections 22 and 28.9";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Void RNG APIs cannot report failure; continuing is a hardening gap"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the same call succeeds without the injected failure";
    spec.metadata.negative_control = "the failure tape is observed to be consumed";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "aigisenc_local_roundtrip";
    spec.algorithm_family = "AIGIS-ENC";
    spec.primitive_type = "kem";
    spec.api_surface = {"keygen", "encaps", "decaps"};
    spec.mutation_type = "none";
    spec.mutation_targets = {};
    spec.expected_relation = ExpectedRelationFromName("SAME_SHARED_SECRET");
    spec.comparator = "bytes_equal";
    spec.triage_policy = "semantic_mismatch_requires_manual_review";
    spec.precondition_json = "{\"same_algorithm\":true,\"valid_lengths\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "For a valid Aigis-Enc key pair, decapsulation shall recover the encapsulated shared secret.";
    spec.metadata.evidence_class = EvidenceClass::kImplementationObserved;
    spec.metadata.source_reference = "PQMagic snapshot kem/aigis-enc/std/kem.c; DeepSeek design doc Section 33";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"No independent Aigis specification was supplied", "Round-trip agreement is not an IND-CCA result"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the baseline encapsulation succeeds";
    spec.metadata.negative_control = "a one-byte ciphertext mutation is exercised by the tampered-ciphertext oracle";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "aigisenc_cross_exchange_roundtrip";
    spec.algorithm_family = "AIGIS-ENC";
    spec.primitive_type = "kem";
    spec.api_surface = {"keygen", "encaps", "decaps"};
    spec.mutation_type = "none";
    spec.mutation_targets = {};
    spec.expected_relation = ExpectedRelationFromName("SAME_SHARED_SECRET");
    spec.comparator = "bytes_equal";
    spec.triage_policy = "semantic_mismatch_requires_manual_review";
    spec.precondition_json = "{\"pair_alg_exchange_contract_required\":[\"public_key_exchange\",\"ciphertext_exchange\"]}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "Two Aigis-Enc builds with compatible object formats exchanging keys and ciphertexts shall derive the same shared secret.";
    spec.metadata.evidence_class = EvidenceClass::kImplementationObserved;
    spec.metadata.source_reference = "DeepSeek design doc Sections 32-33 (SM3 vs SHAKE profiles)";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Not scheduled for the SM3-vs-SHAKE pair because the shared secret binds the hash function", "No independent Aigis specification was supplied"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "both local round trips succeed";
    spec.metadata.negative_control = "the pair file declares the exchange contract before the oracle is enabled";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "aigisenc_tampered_ciphertext_implicit_rejection";
    spec.algorithm_family = "AIGIS-ENC";
    spec.primitive_type = "kem";
    spec.api_surface = {"keygen", "encaps", "decaps"};
    spec.mutation_type = "field_aware";
    spec.mutation_targets = {"ciphertext.u", "ciphertext.v"};
    spec.expected_relation = ExpectedRelationFromName("REJECT_OR_DIFFERENT_SHARED_SECRET");
    spec.comparator = "not_equal_to_original_shared_secret_or_reject";
    spec.triage_policy = "potential_crypto_vuln_if_original_secret_returned";
    spec.precondition_json = "{\"valid_ciphertext_before_mutation\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "A tampered Aigis-Enc ciphertext shall not decapsulate to the original shared secret and shall not expose a public reject flag.";
    spec.metadata.evidence_class = EvidenceClass::kImplementationObserved;
    spec.metadata.source_reference = "DeepSeek design doc Sections 33.2 and 24 (implicit rejection relations)";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"No independent Aigis specification was supplied", "Fallback stability, z-separation, and valid-z independence are separate relations", "This is not an IND-CCA proof"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the unmodified ciphertext decapsulates to the encapsulated secret";
    spec.metadata.negative_control = "the mutation record shows the ciphertext bytes changed";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "aigisenc_bad_randomness_sanity";
    spec.algorithm_family = "AIGIS-ENC";
    spec.primitive_type = "kem";
    spec.api_surface = {"keygen", "encaps"};
    spec.mutation_type = "bad_randomness";
    spec.mutation_targets = {};
    spec.expected_relation = ExpectedRelationFromName("NO_CRASH");
    spec.comparator = "status_only";
    spec.triage_policy = "api_unsupported_is_not_failure";
    spec.precondition_json = "{\"rng_injection_available\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "Aigis-Enc key generation and encapsulation should consume the supplied randomness so that distinct streams produce distinct outputs.";
    spec.metadata.evidence_class = EvidenceClass::kEngineeringRecommendation;
    spec.metadata.source_reference = "DeepSeek design doc Sections 22 and 35 (PQMagic randombytes)";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"No applicable Aigis API requirement for RNG failure recovery was supplied", "The PQMagic randombytes void/abort contract is not exercised by this oracle"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "distinct deterministic tapes are observed to be consumed";
    spec.metadata.negative_control = "the baseline encapsulation succeeds";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "aigisenc_sk_noncanonical_coefficient";
    spec.algorithm_family = "AIGIS-ENC";
    spec.primitive_type = "kem";
    spec.api_surface = {"keygen", "encaps", "decaps"};
    spec.mutation_type = "field_aware";
    spec.mutation_targets = {"secret_key.s_vec"};
    spec.expected_relation = ExpectedRelationFromName("REJECT_OR_INVALID_INPUT");
    spec.comparator = "decode_rejects";
    spec.triage_policy = "implementation_observed_noncanonical_secret_key_acceptance";
    spec.precondition_json = "{\"valid_secret_key_before_mutation\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "A hardened parser profile should reject a secret key whose first s-vector 13-bit coefficient encodes the non-canonical value q=7681 before decapsulation.";
    spec.metadata.evidence_class = EvidenceClass::kEngineeringRecommendation;
    spec.metadata.source_reference = "DeepSeek design doc Section 33.4; PQMagic kem/aigis-enc/std/poly.c";
    spec.metadata.scope_api_layer = "parser";
    spec.metadata.limitations = {"No independent Aigis secret-key canonicality requirement was supplied", "INCONCLUSIVE as a standards verdict; the snapshot observation is acceptance"};
    spec.metadata.conditional_verdict = "NONCONFORMANT if an applicable Aigis specification requires secret-key canonicality or exact re-encoding";
    spec.metadata.positive_control = "the unmutated secret key decapsulates correctly";
    spec.metadata.negative_control = "the mutation record shows the encoded coefficient changed to q";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "aigisenc_implicit_rejection_relations";
    spec.algorithm_family = "AIGIS-ENC";
    spec.primitive_type = "kem";
    spec.api_surface = {"keygen", "encaps", "decaps"};
    spec.mutation_type = "field_aware";
    spec.mutation_targets = {"ciphertext.u", "ciphertext.v", "secret_key.z"};
    spec.expected_relation = ExpectedRelationFromName("REJECT_OR_DIFFERENT_SHARED_SECRET");
    spec.comparator = "relational_implicit_rejection";
    spec.triage_policy = "potential_crypto_vuln_if_fallback_not_bound_to_z";
    spec.precondition_json = "{\"rejection_secret_in_sk\":true,\"valid_ciphertext_before_mutation\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "For a tampered Aigis-Enc ciphertext, decapsulation shall return a stable fallback secret bound to the stored rejection secret z, valid decapsulation shall remain independent of z, and the public status shall not expose a reject flag.";
    spec.metadata.evidence_class = EvidenceClass::kImplementationObserved;
    spec.metadata.source_reference = "DeepSeek design doc Sections 33.2 and 24; PQMagic kem/aigis-enc/std/kem.c";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"No independent Aigis specification was supplied", "Not an IND-CCA proof", "A semantically neutral ciphertext mutation is recorded as no_effect rather than a finding"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the untouched valid ciphertext decapsulates to the encapsulated secret";
    spec.metadata.negative_control = "the mutation records show both the ciphertext and the z region changed";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "aigissig_local_sign_verify";
    spec.algorithm_family = "AIGIS-SIG";
    spec.primitive_type = "sig";
    spec.api_surface = {"keygen", "sign", "verify"};
    spec.mutation_type = "none";
    spec.mutation_targets = {};
    spec.expected_relation = ExpectedRelationFromName("VERIFY_TRUE");
    spec.comparator = "verify_accepts";
    spec.triage_policy = "semantic_mismatch_requires_manual_review";
    spec.precondition_json = "{\"same_algorithm\":true,\"valid_lengths\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "A signature produced by Aigis-Sig with a valid key and context shall verify under the matching public key.";
    spec.metadata.evidence_class = EvidenceClass::kImplementationObserved;
    spec.metadata.source_reference = "PQMagic sig/aigis-sig/std/sign.c; DeepSeek design doc Section 34";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"No independent Aigis specification was supplied", "Verification success is not a sUF-CMA result"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the signature is produced from a freshly generated key pair";
    spec.metadata.negative_control = "a one-byte signature mutation is rejected by the negative oracle";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "aigissig_cross_verify";
    spec.algorithm_family = "AIGIS-SIG";
    spec.primitive_type = "sig";
    spec.api_surface = {"keygen", "sign", "verify"};
    spec.mutation_type = "none";
    spec.mutation_targets = {};
    spec.expected_relation = ExpectedRelationFromName("VERIFY_TRUE");
    spec.comparator = "verify_accepts";
    spec.triage_policy = "semantic_mismatch_requires_manual_review";
    spec.precondition_json = "{\"pair_alg_exchange_contract_required\":[\"public_key_exchange\",\"signature_exchange\"]}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "Two Aigis-Sig builds with compatible object formats exchanging keys and signatures shall cross-verify.";
    spec.metadata.evidence_class = EvidenceClass::kImplementationObserved;
    spec.metadata.source_reference = "DeepSeek design doc Sections 32 and 34";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Not scheduled for the SM3-vs-SHAKE pair because the challenge binds the hash function", "No independent Aigis specification was supplied"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "both local sign/verify paths succeed";
    spec.metadata.negative_control = "the pair file declares the exchange contract before the oracle is enabled";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "aigissig_mutated_signature_negative";
    spec.algorithm_family = "AIGIS-SIG";
    spec.primitive_type = "sig";
    spec.api_surface = {"keygen", "sign", "verify"};
    spec.mutation_type = "field_aware";
    spec.mutation_targets = {"signature.z", "signature.h", "signature.c"};
    spec.expected_relation = ExpectedRelationFromName("VERIFY_FALSE_OR_DECODE_REJECT_OR_API_INVALID_INPUT");
    spec.comparator = "verify_rejects_or_decode_rejects";
    spec.triage_policy = "potential_crypto_vuln_if_mutated_signature_verifies";
    spec.precondition_json = "{\"valid_signature_before_mutation\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "A mutated Aigis-Sig signature component shall not verify under the signing public key.";
    spec.metadata.evidence_class = EvidenceClass::kImplementationObserved;
    spec.metadata.source_reference = "DeepSeek design doc Section 34; PQMagic sig/aigis-sig/std/packing.c";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"No independent Aigis specification was supplied", "Exact-length and unused sign-bit canonicality have dedicated oracles"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the unmodified signature verifies";
    spec.metadata.negative_control = "the mutation record shows the signature bytes changed";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "aigissig_mutated_message_negative";
    spec.algorithm_family = "AIGIS-SIG";
    spec.primitive_type = "sig";
    spec.api_surface = {"keygen", "sign", "verify"};
    spec.mutation_type = "message_mutation";
    spec.mutation_targets = {"message"};
    spec.expected_relation = ExpectedRelationFromName("VERIFY_FALSE");
    spec.comparator = "verify_rejects";
    spec.triage_policy = "potential_crypto_vuln_if_mutated_message_verifies";
    spec.precondition_json = "{\"valid_message_before_mutation\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "An Aigis-Sig signature shall not verify for a message that differs from the signed message.";
    spec.metadata.evidence_class = EvidenceClass::kImplementationObserved;
    spec.metadata.source_reference = "DeepSeek design doc Section 34";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"No independent Aigis specification was supplied"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the original message verifies";
    spec.metadata.negative_control = "the mutation record shows the message bytes changed";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "aigissig_mutated_context_negative";
    spec.algorithm_family = "AIGIS-SIG";
    spec.primitive_type = "sig";
    spec.api_surface = {"keygen", "sign", "verify"};
    spec.mutation_type = "context_mutation";
    spec.mutation_targets = {"ctx"};
    spec.expected_relation = ExpectedRelationFromName("VERIFY_FALSE_OR_API_UNSUPPORTED");
    spec.comparator = "verify_rejects_or_api_unsupported";
    spec.triage_policy = "potential_crypto_vuln_if_mutated_context_verifies";
    spec.precondition_json = "{\"valid_context_before_mutation\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "An Aigis-Sig signature shall not verify when the verification context differs from the signing context.";
    spec.metadata.evidence_class = EvidenceClass::kImplementationObserved;
    spec.metadata.source_reference = "DeepSeek design doc Section 34";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"No independent Aigis specification was supplied", "ctx_len=256 failure-state consistency has a dedicated oracle"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the original context verifies";
    spec.metadata.negative_control = "the mutation record shows the context bytes changed";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "aigissig_bad_randomness_sanity";
    spec.algorithm_family = "AIGIS-SIG";
    spec.primitive_type = "sig";
    spec.api_surface = {"keygen", "sign"};
    spec.mutation_type = "bad_randomness";
    spec.mutation_targets = {};
    spec.expected_relation = ExpectedRelationFromName("NO_CRASH");
    spec.comparator = "status_only";
    spec.triage_policy = "api_unsupported_is_not_failure";
    spec.precondition_json = "{\"rng_injection_available\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "Aigis-Sig key generation and hedged signing should consume the supplied randomness so that distinct streams produce different outputs.";
    spec.metadata.evidence_class = EvidenceClass::kEngineeringRecommendation;
    spec.metadata.source_reference = "DeepSeek design doc Sections 22, 34.4, and 35";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"The supplied snapshot signs deterministically; deterministic profiles are not RNG failures", "No applicable Aigis API requirement for RNG failure recovery was supplied"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "key generation consumes a deterministic tape";
    spec.metadata.negative_control = "the baseline key pair is usable";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "aigissig_exact_length";
    spec.algorithm_family = "AIGIS-SIG";
    spec.primitive_type = "sig";
    spec.api_surface = {"keygen", "sign", "verify"};
    spec.mutation_type = "field_aware";
    spec.mutation_targets = {"signature"};
    spec.expected_relation = ExpectedRelationFromName("VERIFY_FALSE_OR_DECODE_REJECT_OR_API_INVALID_INPUT");
    spec.comparator = "verify_rejects_or_decode_rejects";
    spec.triage_policy = "appended_signature_bytes_accepted";
    spec.precondition_json = "{\"valid_signature_before_mutation\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "A hardened public verifier should require siglen == CRYPTO_BYTES and reject a valid signature with appended bytes.";
    spec.metadata.evidence_class = EvidenceClass::kEngineeringRecommendation;
    spec.metadata.source_reference = "DeepSeek design doc Section 34.2 and case AS-SIG-EXACT-LEN; PQMagic sig/aigis-sig/std/sign.c";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"No independent Aigis specification was supplied; the observation is implementation behavior", "Not a FIPS 204 conformance result"};
    spec.metadata.conditional_verdict = "NONCONFORMANT if an applicable Aigis specification claims exact-length canonical signatures or sUF-CMA";
    spec.metadata.positive_control = "the unmodified signature verifies";
    spec.metadata.negative_control = "the mutation record shows one byte appended";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "aigissig_unused_sign_bits";
    spec.algorithm_family = "AIGIS-SIG";
    spec.primitive_type = "sig";
    spec.api_surface = {"keygen", "sign", "verify"};
    spec.mutation_type = "field_aware";
    spec.mutation_targets = {"signature.c"};
    spec.expected_relation = ExpectedRelationFromName("VERIFY_FALSE_OR_DECODE_REJECT_OR_API_INVALID_INPUT");
    spec.comparator = "verify_rejects_or_decode_rejects";
    spec.triage_policy = "unused_sign_bit_malleable";
    spec.precondition_json = "{\"valid_signature_before_mutation\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "A hardened verifier should reject or canonicalize nonzero unused challenge sign bits rather than accepting a malleable signature encoding.";
    spec.metadata.evidence_class = EvidenceClass::kEngineeringRecommendation;
    spec.metadata.source_reference = "DeepSeek design doc Section 34.3 and case AS-SIG-UNUSED-SIGNBITS; PQMagic sig/aigis-sig/std/packing.c";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"No independent Aigis specification was supplied; the observation is implementation behavior", "Not a FIPS 204 conformance result"};
    spec.metadata.conditional_verdict = "NONCONFORMANT under a byte-string signature model if an applicable Aigis specification claims sUF-CMA";
    spec.metadata.positive_control = "the unmodified signature verifies";
    spec.metadata.negative_control = "the mutation record shows an unused sign bit flipped";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "aigissig_ctx256_failure_state";
    spec.algorithm_family = "AIGIS-SIG";
    spec.primitive_type = "sig";
    spec.api_surface = {"keygen", "sign"};
    spec.mutation_type = "context_mutation";
    spec.mutation_targets = {"ctx"};
    spec.expected_relation = ExpectedRelationFromName("REJECT_WITH_CONSISTENT_OUTPUT_LENGTH_STATE");
    spec.comparator = "failure_state_consistent";
    spec.triage_policy = "failure_output_length_state_inconsistent";
    spec.precondition_json = "{\"valid_keypair_before_mutation\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "A failed detached or combined signing call with ctx_len=256 should report one consistent failure state, leaving caller output length and buffers unchanged or zeroed.";
    spec.metadata.evidence_class = EvidenceClass::kEngineeringRecommendation;
    spec.metadata.source_reference = "DeepSeek design doc Section 34.5 and cases AS-SIG-CTX256-DETACHED-STATE / AS-SIG-CTX256-COMBINED-STATE";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"No independent Aigis specification was supplied; the observation is implementation behavior", "A process abort on invalid context is treated as an observation, not automatically a defect"};
    spec.metadata.conditional_verdict = "NONCONFORMANT if an applicable Aigis API contract specifies output-length state on failure";
    spec.metadata.positive_control = "the same key pair signs successfully with ctx_len=0";
    spec.metadata.negative_control = "the 256-byte context is rejected";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "aigissig_determinism_profile";
    spec.algorithm_family = "AIGIS-SIG";
    spec.primitive_type = "sig";
    spec.api_surface = {"keygen", "sign"};
    spec.mutation_type = "none";
    spec.mutation_targets = {};
    spec.expected_relation = ExpectedRelationFromName("EXPECT_EQUAL");
    spec.comparator = "bytes_equal";
    spec.triage_policy = "determinism_violation";
    spec.precondition_json = "{\"same_algorithm\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "For a deterministic Aigis-Sig profile, identical key, message, and context shall produce byte-identical signatures.";
    spec.metadata.evidence_class = EvidenceClass::kImplementationObserved;
    spec.metadata.source_reference = "DeepSeek design doc Section 34.4 and case AS-SIG-DETERMINISM-PROFILE";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Randomized Aigis-Sig configurations legitimately produce different signatures", "The exact source revision and compile-time options must be recorded"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "both signing calls succeed";
    spec.metadata.negative_control = "signing with a different message changes the signature";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "cross_kat";
    spec.algorithm_family = "CROSS";
    spec.primitive_type = "sig";
    spec.api_surface = {"keygen", "sign", "verify"};
    spec.mutation_type = "none";
    spec.mutation_targets = {};
    spec.expected_relation = ExpectedRelationFromName("EXPECT_EQUAL");
    spec.comparator = "bytes_equal";
    spec.triage_policy = "reference_profile_mismatch";
    spec.precondition_json = "{\"fixture_required\":true,\"same_algorithm\":true,\"valid_lengths\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "For a fixed RNG tape and message, the pinned reference build shall reproduce the recorded CROSS key and signature bytes and shall accept them.";
    spec.metadata.evidence_class = EvidenceClass::kReferenceDerived;
    spec.metadata.source_reference = "CROSS round-2 spec \u00a77 (PDF p.47); Algorithm 1-3; plans/specs/cross-round2-spec.pdf sha256 10ac1ef5";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Fixtures are generated from the pinned upstream reference implementation with a fixed tape, not from an official round-2 KAT file", "A mismatch violates the pinned reference profile, not the CROSS specification by itself"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the recorded fixture decodes to the recorded ABI lengths";
    spec.metadata.negative_control = "a different tape produces different signature bytes";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "cross_local_sign_verify";
    spec.algorithm_family = "CROSS";
    spec.primitive_type = "sig";
    spec.api_surface = {"keygen", "sign", "verify"};
    spec.mutation_type = "none";
    spec.mutation_targets = {};
    spec.expected_relation = ExpectedRelationFromName("VERIFY_TRUE");
    spec.comparator = "verify_accepts";
    spec.triage_policy = "semantic_mismatch_requires_manual_review";
    spec.precondition_json = "{\"same_algorithm\":true,\"valid_lengths\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "A CROSS signature produced by an honest signer shall verify under the matching public key for empty and binary messages.";
    spec.metadata.evidence_class = EvidenceClass::kNormative;
    spec.metadata.source_reference = "CROSS round-2 spec \u00a72.2 Algorithms 1-3 (PDF pp.16-23); \u00a74 Table 4 (PDF p.31)";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Verification success is not an EUF-CMA or sUF-CMA result"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "a freshly generated key pair signs and verifies";
    spec.metadata.negative_control = "a one-byte signature mutation is rejected by cross_exact_lengths and cross_vector_padding";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "cross_cross_verify";
    spec.algorithm_family = "CROSS";
    spec.primitive_type = "sig";
    spec.api_surface = {"keygen", "sign", "verify"};
    spec.mutation_type = "none";
    spec.mutation_targets = {};
    spec.expected_relation = ExpectedRelationFromName("VERIFY_TRUE");
    spec.comparator = "verify_accepts";
    spec.triage_policy = "semantic_mismatch_requires_manual_review";
    spec.precondition_json = "{\"pair_alg_exchange_contract_required\":[\"public_key_exchange\",\"signature_exchange\"]}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "Two pinned CROSS builds of the same variant, category, corner, and revision exchanging keys and signatures shall cross-verify.";
    spec.metadata.evidence_class = EvidenceClass::kNormative;
    spec.metadata.source_reference = "CROSS round-2 spec \u00a72.2 and \u00a74 Table 4; Algorithm 3 verify contract";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Disabled in the pinned single-implementation lock because only one reference build is vendored", "Different optimization corners are not interchangeable positive controls"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "both local sign/verify paths succeed";
    spec.metadata.negative_control = "the pair file declares signature_exchange before the oracle is enabled";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "cross_message_key_binding";
    spec.algorithm_family = "CROSS";
    spec.primitive_type = "sig";
    spec.api_surface = {"keygen", "sign", "verify"};
    spec.mutation_type = "message_mutation";
    spec.mutation_targets = {"message", "public_key"};
    spec.expected_relation = ExpectedRelationFromName("VERIFY_FALSE");
    spec.comparator = "verify_rejects";
    spec.triage_policy = "potential_crypto_vuln_if_mutated_message_verifies";
    spec.precondition_json = "{\"valid_signature_before_mutation\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "A CROSS signature shall not verify for a message that differs from the signed message, nor under an independent honest public key.";
    spec.metadata.evidence_class = EvidenceClass::kNormative;
    spec.metadata.source_reference = "CROSS round-2 spec \u00a72.2 Algorithm 3 (PDF pp.21-23)";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Failure to verify under another honest key is not a rogue-key proof", "A valid signature on a new message is not automatically an EUF forgery"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the original message under the original key verifies";
    spec.metadata.negative_control = "the mutation record shows the message or public key bytes changed";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "cross_exact_lengths";
    spec.algorithm_family = "CROSS";
    spec.primitive_type = "sig";
    spec.api_surface = {"keygen", "sign", "verify"};
    spec.mutation_type = "length_boundary";
    spec.mutation_targets = {"signature"};
    spec.expected_relation = ExpectedRelationFromName("VERIFY_FALSE_OR_DECODE_REJECT_OR_API_INVALID_INPUT");
    spec.comparator = "verify_rejects_or_decode_rejects";
    spec.triage_policy = "appended_signature_bytes_accepted";
    spec.precondition_json = "{\"valid_signature_before_mutation\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "The CROSS verifier shall require the exact CROSS_sig_t length and reject truncated or appended signature byte strings.";
    spec.metadata.evidence_class = EvidenceClass::kNormative;
    spec.metadata.source_reference = "CROSS round-2 spec \u00a72.2.2 signature sizes (PDF p.23); \u00a75.4 packing (PDF pp.43-44); api.h CRYPTO_BYTES";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Length-aware wrapper observations are labelled api_layer=adapter"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the unmodified signature verifies";
    spec.metadata.negative_control = "the mutation record shows one byte appended or removed";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "cross_packed_field_range";
    spec.algorithm_family = "CROSS";
    spec.primitive_type = "sig";
    spec.api_surface = {"keygen", "sign", "verify"};
    spec.mutation_type = "field_aware";
    spec.mutation_targets = {"signature.y", "signature.v", "signature.path"};
    spec.expected_relation = ExpectedRelationFromName("VERIFY_FALSE_OR_DECODE_REJECT_OR_API_INVALID_INPUT");
    spec.comparator = "verify_rejects_or_decode_rejects";
    spec.triage_policy = "out_of_domain_wire_coefficient_accepted";
    spec.precondition_json = "{\"valid_signature_before_mutation\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "Packed y, v, and vG coefficients outside Fp/Fz shall not be accepted as zero or silently reduced: decapsulation of every wire representation must be canonical.";
    spec.metadata.evidence_class = EvidenceClass::kNormative;
    spec.metadata.source_reference = "CROSS round-2 spec Algorithm 3 membership checks; \u00a75.4 packing (PDF pp.43-44); \u00a75.5 arithmetic (PDF p.45); pack_unpack.c unpack_fp_vec";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Internal Mersenne redundant-zero representations are not wire-level coefficients"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the unmodified in-domain vector verifies";
    spec.metadata.negative_control = "the mutation record marks the target coefficient out of range";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "cross_vector_padding";
    spec.algorithm_family = "CROSS";
    spec.primitive_type = "sig";
    spec.api_surface = {"keygen", "sign", "verify"};
    spec.mutation_type = "bit_padding";
    spec.mutation_targets = {"signature.y", "signature.v", "signature.syndrome"};
    spec.expected_relation = ExpectedRelationFromName("VERIFY_FALSE_OR_DECODE_REJECT_OR_API_INVALID_INPUT");
    spec.comparator = "verify_rejects_or_decode_rejects";
    spec.triage_policy = "unused_padding_bits_accepted";
    spec.precondition_json = "{\"valid_signature_before_mutation\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "Each densely packed vector is zero-padded to a byte boundary; a decoder shall reject non-zero unused padding bits rather than deriving the same value from two encodings.";
    spec.metadata.evidence_class = EvidenceClass::kNormative;
    spec.metadata.source_reference = "CROSS round-2 spec \u00a75.4 packing requires zero padding (PDF pp.43-44); pack_unpack.c unpack checks";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Byte-aligned vectors have no padding case"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the unmodified signature verifies";
    spec.metadata.negative_control = "the mutation targets a bit above the coefficient bit width";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "cross_challenge_sampling";
    spec.algorithm_family = "CROSS";
    spec.primitive_type = "sig";
    spec.api_surface = {"keygen", "sign", "verify"};
    spec.mutation_type = "digest_mutation";
    spec.mutation_targets = {"signature.digest_chall2"};
    spec.expected_relation = ExpectedRelationFromName("EXPECT_EQUAL");
    spec.comparator = "model_challenge_equal";
    spec.triage_policy = "challenge_sampler_divergence";
    spec.precondition_json = "{\"valid_signature_before_mutation\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "The fixed-weight challenge shall be derived from the digest by the specified sampler: chall1 entries are nonzero, chall2 has Hamming weight exactly w, and an independent model shall reproduce the same bit string.";
    spec.metadata.evidence_class = EvidenceClass::kNormative;
    spec.metadata.source_reference = "CROSS round-2 spec \u00a72.3 Table 3 (PDF pp.23-24); expand_digest_to_fixed_weight in csprng_hash.c";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Only the rejection sampler path is exercised; probability histograms alone do not establish the predicate"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the model sampler reproduces the reference challenge for the fixed digest";
    spec.metadata.negative_control = "a digest mutation changes the challenge";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "cross_commitment_digests";
    spec.algorithm_family = "CROSS";
    spec.primitive_type = "sig";
    spec.api_surface = {"keygen", "sign", "verify"};
    spec.mutation_type = "field_aware";
    spec.mutation_targets = {"signature.digest_cmt", "signature.digest_chall2", "signature.resp1"};
    spec.expected_relation = ExpectedRelationFromName("VERIFY_FALSE_OR_DECODE_REJECT_OR_API_INVALID_INPUT");
    spec.comparator = "verify_rejects_or_decode_rejects";
    spec.triage_policy = "commitment_digest_malleability";
    spec.precondition_json = "{\"valid_signature_before_mutation\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "Verification shall recompute both final digests and accept if and only if they match the committed roots; changing digest_cmt, digest_chall2, or any response shall reject.";
    spec.metadata.evidence_class = EvidenceClass::kNormative;
    spec.metadata.source_reference = "CROSS round-2 spec \u00a72.2 Algorithm 3 lines 22-28 (PDF pp.22-23)";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"The independent recomputation is supplied by the Python transcript model; the native oracle observes rejection"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the unmodified signature verifies";
    spec.metadata.negative_control = "each targeted field mutation is recorded as effective";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "cross_domain_transcript";
    spec.algorithm_family = "CROSS";
    spec.primitive_type = "sig";
    spec.api_surface = {"sign", "verify"};
    spec.mutation_type = "field_aware";
    spec.mutation_targets = {"signature.salt", "signature.digest_cmt"};
    spec.expected_relation = ExpectedRelationFromName("EXPECT_EQUAL");
    spec.comparator = "model_transcript_equal";
    spec.triage_policy = "domain_separation_divergence";
    spec.precondition_json = "{\"valid_signature_before_mutation\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "Every Hash/CSPRNG call shall use the specified little-endian 16-bit role tag and digest length; the transcript model with the same tags shall reproduce the reference digests.";
    spec.metadata.evidence_class = EvidenceClass::kNormative;
    spec.metadata.source_reference = "CROSS round-2 spec \u00a75.1 domain separation (PDF pp.32-33) and \u00a72.2";
    spec.metadata.scope_api_layer = "internal";
    spec.metadata.limitations = {"Specification \u00a72.2.2 cmt1 text omits Salt while Algorithms 2-3 include it; the model follows the algorithms", "Tags are recorded explicitly rather than adding or subtracting one globally"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the model reproduces the reference digest for unchanged inputs";
    spec.metadata.negative_control = "a big-endian tag model diverges";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "cross_seed_rebuild";
    spec.algorithm_family = "CROSS";
    spec.primitive_type = "sig";
    spec.api_surface = {"sign"};
    spec.mutation_type = "tree_flip";
    spec.mutation_targets = {"signature.path"};
    spec.expected_relation = ExpectedRelationFromName("EXPECT_EQUAL");
    spec.comparator = "model_seed_set_equal";
    spec.triage_policy = "unexpected_seed_disclosure";
    spec.precondition_json = "{\"valid_signature_before_mutation\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "RebuildLeaves(SeedPath(seed, salt, b), b, salt) shall recover exactly the published leaves, and the published node set shall equal the zero-challenge round leaves and never cover one-challenge rounds.";
    spec.metadata.evidence_class = EvidenceClass::kNormative;
    spec.metadata.source_reference = "CROSS round-2 spec \u00a75.2 SeedTree algorithms (PDF pp.34-42); Algorithms 5-10";
    spec.metadata.scope_api_layer = "internal";
    spec.metadata.limitations = {"Secret leaves are never written to ordinary reports"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the reference path rebuilds the honest round leaves";
    spec.metadata.negative_control = "a flipped zero-challenge flag tree is detected by the published-set predicate";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "cross_merkle_proof";
    spec.algorithm_family = "CROSS";
    spec.primitive_type = "sig";
    spec.api_surface = {"verify"};
    spec.mutation_type = "field_aware";
    spec.mutation_targets = {"signature.proof", "signature.path"};
    spec.expected_relation = ExpectedRelationFromName("VERIFY_FALSE_OR_DECODE_REJECT_OR_API_INVALID_INPUT");
    spec.comparator = "verify_rejects_or_decode_rejects";
    spec.triage_policy = "merkle_proof_acceptance";
    spec.precondition_json = "{\"valid_signature_before_mutation\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "Changing an active Proof node, reordering active proof nodes, or corrupting an active Path seed shall make the recomputed root mismatch and verification fail.";
    spec.metadata.evidence_class = EvidenceClass::kNormative;
    spec.metadata.source_reference = "CROSS round-2 spec \u00a75.2 Merkle tree algorithms 11-16 (PDF pp.38-42)";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Fast, balanced, and small tree topologies are tested separately; unused slots are covered by cross_path_proof_consumption"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the honest proof recomputes the honest root";
    spec.metadata.negative_control = "the mutation record distinguishes path from proof targets";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "cross_path_proof_consumption";
    spec.algorithm_family = "CROSS";
    spec.primitive_type = "sig";
    spec.api_surface = {"verify"};
    spec.mutation_type = "field_aware";
    spec.mutation_targets = {"signature.path", "signature.proof", "signature.unused_slots"};
    spec.expected_relation = ExpectedRelationFromName("EXPECT_EQUAL");
    spec.comparator = "consumption_record_equal";
    spec.triage_policy = "path_proof_index_out_of_bounds";
    spec.precondition_json = "{\"valid_signature_before_mutation\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "Path and Proof consumption shall be decided by the challenge topology and shall match the recorded counts without out-of-bounds or duplicate consumption; unused fixed slots follow the pinned serializer contract.";
    spec.metadata.evidence_class = EvidenceClass::kImplementationObserved;
    spec.metadata.source_reference = "CROSS round-2 spec \u00a72.2.2 bound and \u00a75.2 tree shape (PDF pp.34-42)";
    spec.metadata.scope_api_layer = "api";
    spec.metadata.limitations = {"Remaining-byte rules are not fully specified in text; behaviour is reported as an implementation observation", "Unused-slot zeroization is a serializer property, not \u00a75.4 vector padding"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the honest signature consumes exactly the topology counts";
    spec.metadata.negative_control = "an inconsistent challenge/topology fixture consumes a different count";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "cross_key_algebra";
    spec.algorithm_family = "CROSS";
    spec.primitive_type = "sig";
    spec.api_surface = {"keygen", "sign"};
    spec.mutation_type = "field_aware";
    spec.mutation_targets = {"public_key.syndrome", "public_key.seed"};
    spec.expected_relation = ExpectedRelationFromName("EXPECT_EQUAL");
    spec.comparator = "model_syndrome_equal";
    spec.triage_policy = "key_algebra_mismatch";
    spec.precondition_json = "{\"same_algorithm\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "ExpandSK shall rederive Seedpk and the parity matrix, and the public syndrome shall equal e*H^T (RSDP) or eG*M mod z (RSDPG) under the specified row/column order.";
    spec.metadata.evidence_class = EvidenceClass::kNormative;
    spec.metadata.source_reference = "CROSS round-2 spec Algorithm 1 and Algorithm 4 (PDF pp.16-17); \u00a75.3-5.4 arithmetic";
    spec.metadata.scope_api_layer = "internal";
    spec.metadata.limitations = {"Row/column order is checked against a fixed fixture; a mismatch is reported before any target verdict"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the reference key satisfies the model syndrome equation";
    spec.metadata.negative_control = "a mutated pk seed breaks it";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "cross_rng_replay";
    spec.algorithm_family = "CROSS";
    spec.primitive_type = "sig";
    spec.api_surface = {"keygen", "sign"};
    spec.mutation_type = "bad_randomness";
    spec.mutation_targets = {};
    spec.expected_relation = ExpectedRelationFromName("EXPECT_EQUAL");
    spec.comparator = "bytes_equal";
    spec.triage_policy = "rng_contract_violation";
    spec.precondition_json = "{\"rng_injection_available\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "Key generation and signing shall consume the injected CSPRNG tape deterministically: the same tape reproduces identical bytes on the same build, and a fresh legal tape yields a verifying signature.";
    spec.metadata.evidence_class = EvidenceClass::kEngineeringRecommendation;
    spec.metadata.source_reference = "CROSS round-2 spec Algorithm 1-2; csprng_hash.c platform_csprng_state; api.h CRYPTO_RANDOMBYTES";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Repeated salt or seed is a diagnostic observation, not automatically a normative rejection", "Same-tape equality only holds for the same build and call path"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "two runs under the same tape agree";
    spec.metadata.negative_control = "a different tape changes the salt";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "cross_failure_resources";
    spec.algorithm_family = "CROSS";
    spec.primitive_type = "sig";
    spec.api_surface = {"keygen", "sign", "verify"};
    spec.mutation_type = "failure_injection";
    spec.mutation_targets = {"length", "allocation", "rng"};
    spec.expected_relation = ExpectedRelationFromName("REJECT_OR_INVALID_INPUT");
    spec.comparator = "status_or_length_state";
    spec.triage_policy = "failure_output_state_inconsistent";
    spec.precondition_json = "{\"api_available\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "Failure paths shall report a consistent status and leave caller output length and buffers bounded by the sentinel/canary contract; parser and allocation budgets shall stay within the configured limits.";
    spec.metadata.evidence_class = EvidenceClass::kEngineeringRecommendation;
    spec.metadata.source_reference = "CROSS round-2 spec API contract; harness common API contract \u00a71.4";
    spec.metadata.scope_api_layer = "api";
    spec.metadata.limitations = {"The production retry policy is not modified; allocation-failure probes report the observed state only"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the honest path returns success with exact lengths";
    spec.metadata.negative_control = "a poisoned output sentinel is detected";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "cross_parallel_arithmetic";
    spec.algorithm_family = "CROSS";
    spec.primitive_type = "sig";
    spec.api_surface = {"sign", "verify"};
    spec.mutation_type = "lane_batch";
    spec.mutation_targets = {"lane_count"};
    spec.expected_relation = ExpectedRelationFromName("EXPECT_EQUAL");
    spec.comparator = "bytes_equal";
    spec.triage_policy = "batch_lane_divergence";
    spec.precondition_json = "{\"same_algorithm\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "Scalar and batch SHAKE calls shall agree for 1, 2, 3, and 4 lanes including short tail batches; Fp/Fz arithmetic shall agree with independent modular arithmetic on small-domain and boundary operands.";
    spec.metadata.evidence_class = EvidenceClass::kNormative;
    spec.metadata.source_reference = "CROSS round-2 spec \u00a75.3-5.5 (PDF pp.44-46); fips202.c and fp_arith.h";
    spec.metadata.scope_api_layer = "internal";
    spec.metadata.limitations = {"Variable public inputs do not by themselves constitute leakage evidence"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "lane counts that divide the round total agree with the scalar result";
    spec.metadata.negative_control = "an intentionally mis-sized tail batch diverges";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "cross_fault_seed_disclosure";
    spec.algorithm_family = "CROSS";
    spec.primitive_type = "sig";
    spec.api_surface = {"sign"};
    spec.mutation_type = "fault_injection";
    spec.mutation_targets = {"flag_tree", "challenge_bit"};
    spec.expected_relation = ExpectedRelationFromName("VERIFY_FALSE_OR_DECODE_REJECT_OR_API_INVALID_INPUT");
    spec.comparator = "verify_rejects_or_decode_rejects";
    spec.triage_policy = "seed_and_response_same_round_disclosed";
    spec.precondition_json = "{\"valid_signature_before_mutation\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "A fault that flips the flag tree or shifts a challenge bit shall not cause the same round to disclose both its seed and its zero-branch response; such a mutant must be detected by cross_seed_rebuild.";
    spec.metadata.evidence_class = EvidenceClass::kEngineeringRecommendation;
    spec.metadata.source_reference = "CROSS round-2 spec \u00a75.6 fault-attack boundary (PDF pp.45-46)";
    spec.metadata.scope_api_layer = "internal";
    spec.metadata.limitations = {"A signature that merely fails to verify is not a leakage detector"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the fault-free signature verifies";
    spec.metadata.negative_control = "the synthetic mutant is caught by the published-seed-set predicate";
    specs.push_back(std::move(spec));
  }
  {
    OracleSpec spec;
    spec.oracle_id = "cross_timing";
    spec.algorithm_family = "CROSS";
    spec.primitive_type = "sig";
    spec.api_surface = {"sign", "verify"};
    spec.mutation_type = "timing_probe";
    spec.mutation_targets = {"secret_syndrome", "exponentiation", "public_verify"};
    spec.expected_relation = ExpectedRelationFromName("NO_TIMEOUT");
    spec.comparator = "timing_distribution_pair";
    spec.triage_policy = "timing_signal_requires_manual_review";
    spec.precondition_json = "{\"timing_lane_available\":true}";
    spec.enabled_by_default = true;
    spec.requires_api = "";
    spec.metadata.claim = "The timing lane shall complete within the configured budget while comparing secret-dependent and public verification paths; no timing conclusion is drawn without the dedicated non-sanitized binary and multiple-comparison correction.";
    spec.metadata.evidence_class = EvidenceClass::kInference;
    spec.metadata.source_reference = "CROSS round-2 spec \u00a75.5-5.6 (PDF pp.45-46); harness common \u00a71.4";
    spec.metadata.scope_api_layer = "external";
    spec.metadata.limitations = {"Opt-in P2 lane; not part of the default smoke contract", "No power or EM claim is made without a physical measurement setup"};
    spec.metadata.conditional_verdict = "";
    spec.metadata.positive_control = "the public verify path is measured in the same harness";
    spec.metadata.negative_control = "the lane reports not_applicable when the timing binary is absent";
    specs.push_back(std::move(spec));
  }
  return specs;
}
